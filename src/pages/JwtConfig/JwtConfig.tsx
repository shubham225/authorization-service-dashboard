import { Box } from '@mui/material'
import Header from 'components/Header'
import React from 'react'

export const JwtConfig = () => {
  return (
    <Box>
      <Header title='JWT CONFIGURATION' subtitle='Configure JWT tokens' />
      <div>JwtConfig</div>
    </Box>
  )
}
